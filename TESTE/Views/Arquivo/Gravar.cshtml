@using (@Html.BeginForm("GravarArquivo", "Arquivo", FormMethod.Post, new { @class = "form", @id = "frmLogin", enctype = "multipart/form-data" }))
{

    <div class="container-fluid">
        <div class="row">
            <div class="col-md-8">
                <input type="file" name="file" />
            </div>
            <div class="col-md-4">
                <input type="submit" />
            </div>
        </div>
    </div>

}